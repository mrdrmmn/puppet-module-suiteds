# Class: directory_server::config
#
# This will never have anything in it by default.  It exists for the sole
# purpose of giving end-users a place to put site overrides without needing
# to modify other parts of this module.
class suiteds::config inherits suiteds::config::os {
}
